# =============================================================================
# DataScope Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Databricks Workspace (Required)
# -----------------------------------------------------------------------------
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
DATABRICKS_TOKEN=dapi_your_token_here
DATABRICKS_SQL_WAREHOUSE_ID=your_warehouse_id

# -----------------------------------------------------------------------------
# LLM Configuration (Choose ONE option)
# -----------------------------------------------------------------------------

# Option A: Databricks External Endpoint (Recommended for production)
# Use Claude via Databricks Model Serving with AI Gateway
LLM_ENDPOINT_NAME=databricks-claude-sonnet

# Option B: Direct Anthropic API (For local development only)
# Uncomment if you don't have External Endpoint set up yet
# ANTHROPIC_API_KEY=sk-ant-your_key_here

# -----------------------------------------------------------------------------
# State Management (Optional - for multi-turn conversations)
# -----------------------------------------------------------------------------
# Lakebase PostgreSQL connection string
# LAKEBASE_CONNECTION_STRING=postgresql://user:pass@host:5432/dbname

# -----------------------------------------------------------------------------
# GitHub MCP App (Optional - for code search)
# -----------------------------------------------------------------------------
# URL of the deployed GitHub MCP Databricks App
# GITHUB_MCP_APP_URL=https://github-mcp-server.your-workspace.databricksapps.com

# GitHub token for the MCP app (set as app secret, not here)
# GITHUB_PERSONAL_ACCESS_TOKEN=ghp_your_token

# -----------------------------------------------------------------------------
# Catalog Configuration
# -----------------------------------------------------------------------------
DATASCOPE_CATALOG=novatech
DATASCOPE_SCHEMA_BRONZE=bronze
DATASCOPE_SCHEMA_SILVER=silver
DATASCOPE_SCHEMA_GOLD=gold

# -----------------------------------------------------------------------------
# MLflow (Optional)
# -----------------------------------------------------------------------------
MLFLOW_TRACKING_URI=databricks
